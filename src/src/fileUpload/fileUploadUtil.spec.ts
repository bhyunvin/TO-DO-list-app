import { describe, it, expect } from 'bun:test';
import { FileUploadUtil } from './fileUploadUtil';

describe('FileUploadUtil', () => {
  it('FileUploadUtil이 정의되어 있어야 함', () => {
    expect(FileUploadUtil).toBeDefined();
  });
});
